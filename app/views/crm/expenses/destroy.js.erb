<% if @expense.errors.any? %>
  swal('Error', '<%= @expense.errors.full_messages.to_sentence%>', 'error')
<% else %>
  Turbolinks.visit(window.location, { action: 'replace' })

  setTimeout(function(){
    toastr.success('Operación realizada con exito!', { timeOut: 2000 })
  }, 500);

<% end %>
