<% if @report.errors.any? %>
  $('#reportPriceModal').html('<%= j(render(partial: 'form').html_safe)%>');
  $('#reportPriceModal').modal('show')
<% else %>
  Turbolinks.visit(window.location, { action: 'replace' })
<% end %>
